"""
Write a function that takes a string as input and returns the string reversed
"""
#1. O(n) runtime, O(n) space
class Solution(object):
    def reverseString(self, s):
        """
        :type s: str
        :rtype: str
        """
        output = []
        l = len(s)
        for i in range(l):
            output.append(s[l-1-i])
        return ''.join(output)
